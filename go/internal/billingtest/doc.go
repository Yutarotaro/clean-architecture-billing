// Package billingtest は複数の永続化実装に対して同じテストを回すための場所。
//
// 「インターフェースが同じ」だけでは意味がなく、「同じように振る舞う」ことが必要である。
// ここのテストが memory と SQLite の両方で通ることが、ユースケース層が永続化技術から
// 本当に独立していることの根拠になる。
//
// 実装は _test.go にしかない。本番のバイナリには 1 バイトも入らない。
package billingtest
